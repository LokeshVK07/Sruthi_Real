name: Background Refresh Telugu

on:
  workflow_dispatch:
    inputs:
      full_refresh:
        description: Run a full catalog refresh instead of incremental mode
        required: false
        default: false
        type: boolean
  schedule:
    - cron: "17 */6 * * *"

permissions:
  contents: read
  actions: write

concurrency:
  group: sruthi-background-refresh-telugu
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  refresh-and-deploy:
    name: Refresh and Deploy Telugu
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      GH_VARIABLES_TOKEN: ${{ secrets.GH_VARIABLES_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      SRUTHI_TELUGU_ACTIVE_D1_SLOT: ${{ vars.SRUTHI_TELUGU_ACTIVE_D1_SLOT }}
      SRUTHI_TELUGU_D1_DB_A_ID: ${{ vars.SRUTHI_TELUGU_D1_DB_A_ID }}
      SRUTHI_TELUGU_D1_DB_A_NAME: ${{ vars.SRUTHI_TELUGU_D1_DB_A_NAME }}
      SRUTHI_TELUGU_D1_DB_B_ID: ${{ vars.SRUTHI_TELUGU_D1_DB_B_ID }}
      SRUTHI_TELUGU_D1_DB_B_NAME: ${{ vars.SRUTHI_TELUGU_D1_DB_B_NAME }}
      WRANGLER_SEND_METRICS: "false"
      GENERATED_WRANGLER_CONFIG: cloudflare/.generated/wrangler.deploy.telugu.jsonc
      RELEASE_MANIFEST: cloudflare/.generated/release-manifest-telugu.json
      RELEASE_DUCKDB: cloudflare/.generated/release-check-telugu.duckdb
      TELUGU_DATA_DIR: data/telugu
      TELUGU_MEDIA_DIR: media/telugu
      TELUGU_CACHE_DIR: .cache/audio-telugu
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install workflow dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install duckdb requests beautifulsoup4 cloudscraper
          npm install --global wrangler@4.81.1

      - name: Validate workflow configuration
        run: |
          set -euo pipefail

          missing=()
          for key in \
            CLOUDFLARE_API_TOKEN \
            GH_VARIABLES_TOKEN \
            CLOUDFLARE_ACCOUNT_ID \
            SRUTHI_TELUGU_D1_DB_A_ID \
            SRUTHI_TELUGU_D1_DB_A_NAME \
            SRUTHI_TELUGU_D1_DB_B_ID \
            SRUTHI_TELUGU_D1_DB_B_NAME; do
            if [[ -z "${!key:-}" ]]; then
              missing+=("$key")
            fi
          done

          if (( ${#missing[@]} > 0 )); then
            echo "::error::Missing required GitHub Actions secrets/variables: ${missing[*]}"
            exit 1
          fi

          if [[ "${SRUTHI_TELUGU_D1_DB_A_ID}" == "${SRUTHI_TELUGU_D1_DB_B_ID}" ]]; then
            echo "::error::Telugu D1 slot A and B database ids must be different."
            exit 1
          fi

          if [[ "${SRUTHI_TELUGU_D1_DB_A_NAME}" == "${SRUTHI_TELUGU_D1_DB_B_NAME}" ]]; then
            echo "::error::Telugu D1 slot A and B database names must be different."
            exit 1
          fi

          if [[ -n "${SRUTHI_TELUGU_ACTIVE_D1_SLOT:-}" && "${SRUTHI_TELUGU_ACTIVE_D1_SLOT}" != "A" && "${SRUTHI_TELUGU_ACTIVE_D1_SLOT}" != "B" ]]; then
            echo "::error::SRUTHI_TELUGU_ACTIVE_D1_SLOT must be A or B when configured."
            exit 1
          fi

          required_files=(
            ".github/workflows/background-refresh-telugu.yml"
            "cloudflare/wrangler.telugu.jsonc"
            "cloudflare/src/worker.js"
            "cloudflare/public/index.html"
            "cloudflare/public/app.js"
            "cloudflare/public/styles.css"
            "cloudflare/scripts/render_wrangler_config.py"
            "cloudflare/scripts/prepare_release.py"
            "cloudflare/scripts/export_d1_sql.py"
            "cloudflare/data/release-baseline-telugu.json"
            "tools/masstelugu_refresh.py"
            "server.py"
          )

          for file in "${required_files[@]}"; do
            if [[ ! -e "$file" ]]; then
              echo "::error::Required workflow file is missing: $file"
              exit 1
            fi
          done

      - name: Resolve run mode
        run: |
          set -euo pipefail
          full_refresh="false"
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" && "${{ inputs.full_refresh }}" == "true" ]]; then
            full_refresh="true"
          fi
          echo "SRUTHI_FULL_REFRESH=${full_refresh}" >> "$GITHUB_ENV"

      - name: Verify Cloudflare token and account access
        run: |
          set -euo pipefail
          if ! npx wrangler whoami >/tmp/wrangler-whoami.txt 2>/tmp/wrangler-whoami.err; then
            echo "::error::Wrangler authentication check failed. Ensure CLOUDFLARE_API_TOKEN includes Workers, D1, and account membership access, and CLOUDFLARE_ACCOUNT_ID is correct."
            cat /tmp/wrangler-whoami.err
            exit 1
          fi

      - name: Select inactive Telugu D1 slot
        run: |
          set -euo pipefail
          active="${SRUTHI_TELUGU_ACTIVE_D1_SLOT:-A}"
          if [[ "$active" == "A" ]]; then
            target="B"
            db_id="${SRUTHI_TELUGU_D1_DB_B_ID}"
            db_name="${SRUTHI_TELUGU_D1_DB_B_NAME}"
          else
            target="A"
            db_id="${SRUTHI_TELUGU_D1_DB_A_ID}"
            db_name="${SRUTHI_TELUGU_D1_DB_A_NAME}"
          fi

          echo "TARGET_DB_ID=$db_id" >> "$GITHUB_ENV"
          echo "TARGET_DB_NAME=$db_name" >> "$GITHUB_ENV"
          echo "TARGET_SLOT=$target" >> "$GITHUB_ENV"

      - name: Verify GitHub variable update token access
        env:
          GH_TOKEN: ${{ secrets.GH_VARIABLES_TOKEN }}
        run: |
          set -euo pipefail

          if [[ -z "${GH_TOKEN:-}" ]]; then
            echo "::error::Missing required secret GH_VARIABLES_TOKEN. Create this repository secret before running the workflow."
            exit 1
          fi

          if [[ "${TARGET_SLOT}" != "A" && "${TARGET_SLOT}" != "B" ]]; then
            echo "::error::Resolved TARGET_SLOT must be A or B, got '${TARGET_SLOT}'."
            exit 1
          fi

          if ! gh auth status >/tmp/gh-auth.txt 2>/tmp/gh-auth.err; then
            echo "::error::GH_VARIABLES_TOKEN could not authenticate with GitHub CLI."
            cat /tmp/gh-auth.err
            exit 1
          fi

          set +e
          gh api "repos/${GITHUB_REPOSITORY}/actions/variables/SRUTHI_TELUGU_ACTIVE_D1_SLOT" >/tmp/slot-var.json 2>/tmp/slot-var.err
          status=$?
          set -e

          if [[ $status -eq 0 ]]; then
            echo "Verified repository variable API access for SRUTHI_TELUGU_ACTIVE_D1_SLOT."
          elif grep -q "404" /tmp/slot-var.err; then
            echo "SRUTHI_TELUGU_ACTIVE_D1_SLOT does not exist yet; workflow will create it after a successful deploy."
          else
            echo "::error::GH_VARIABLES_TOKEN does not have the required repository variables access."
            cat /tmp/slot-var.err
            exit 1
          fi

      - name: Render deployment Wrangler config
        run: |
          set -euo pipefail
          mkdir -p "$(dirname "${GENERATED_WRANGLER_CONFIG}")"
          python cloudflare/scripts/render_wrangler_config.py \
            --input cloudflare/wrangler.telugu.jsonc \
            --output "${GENERATED_WRANGLER_CONFIG}" \
            --database-id "${TARGET_DB_ID}" \
            --database-name "${TARGET_DB_NAME}" \
            --account-id "${CLOUDFLARE_ACCOUNT_ID}"

      - name: Validate rendered deployment config
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          config_path = Path(os.environ["GENERATED_WRANGLER_CONFIG"])
          payload = json.loads(config_path.read_text(encoding="utf-8"))

          main_path = Path(payload.get("main", ""))
          assets_dir = Path((payload.get("assets") or {}).get("directory", ""))
          d1_databases = payload.get("d1_databases") or []

          if not main_path.is_file():
            raise SystemExit(f"Rendered Wrangler main entry is invalid: {main_path}")
          if not assets_dir.is_dir():
            raise SystemExit(f"Rendered Wrangler assets directory is invalid: {assets_dir}")
          if not d1_databases:
            raise SystemExit("Rendered Wrangler config has no d1_databases entry.")
          database = d1_databases[0]
          if not database.get("database_id") or not database.get("database_name"):
            raise SystemExit("Rendered Wrangler config is missing D1 database_id or database_name.")

          print({
            "main": str(main_path),
            "assets": str(assets_dir),
            "database_name": database["database_name"],
            "database_id": database["database_id"],
          })
          PY

      - name: Refresh Telugu catalog from MassTelugu
        env:
          SRUTHI_DATA_DIR: ${{ env.TELUGU_DATA_DIR }}
          SRUTHI_MEDIA_DIR: ${{ env.TELUGU_MEDIA_DIR }}
          SRUTHI_CACHE_AUDIO_DIR: ${{ env.TELUGU_CACHE_DIR }}
        run: |
          set -euo pipefail
          mkdir -p "${SRUTHI_DATA_DIR}" "${SRUTHI_MEDIA_DIR}" "${SRUTHI_CACHE_AUDIO_DIR}"
          python tools/masstelugu_refresh.py \
            --include-tag-index \
            --workers 1 \
            --batch-size 1 \
            --page-delay 0.6 \
            --album-delay 1.2 \
            --retry-count 5 \
            --retry-base-delay 5 \
            --full

      - name: Validate refreshed local Telugu catalog files
        run: |
          set -euo pipefail
          if [[ ! -f data/telugu/sruthi.db ]]; then
            echo "::error::Expected data/telugu/sruthi.db to exist after refresh."
            exit 1
          fi
          if [[ ! -s data/telugu/sruthi.db ]]; then
            echo "::error::data/telugu/sruthi.db exists but is empty."
            exit 1
          fi

      - name: Validate Telugu catalog and build release seed
        run: |
          set -euo pipefail
          python cloudflare/scripts/prepare_release.py \
            --db data/telugu/sruthi.db \
            --seed cloudflare/data/seed-telugu.sql \
            --baseline cloudflare/data/release-baseline-telugu.json \
            --manifest "${RELEASE_MANIFEST}" \
            --duckdb-path "${RELEASE_DUCKDB}"

      - name: Upload Telugu build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sruthi-telugu-release
          if-no-files-found: error
          path: |
            cloudflare/data/seed-telugu.sql
            ${{ env.RELEASE_MANIFEST }}

      - name: Import Telugu release into inactive D1 slot
        run: |
          set -euo pipefail
          npx wrangler d1 execute "${TARGET_DB_NAME}" \
            --remote \
            --file cloudflare/data/seed-telugu.sql \
            --config "${GENERATED_WRANGLER_CONFIG}"

      - name: Validate inactive Telugu D1 slot
        run: |
          set -euo pipefail
          npx wrangler d1 execute "${TARGET_DB_NAME}" \
            --remote \
            --command "SELECT COUNT(*) AS count FROM albums;" \
            --config "${GENERATED_WRANGLER_CONFIG}" \
            --json > /tmp/albums.json
          npx wrangler d1 execute "${TARGET_DB_NAME}" \
            --remote \
            --command "SELECT COUNT(*) AS count FROM songs;" \
            --config "${GENERATED_WRANGLER_CONFIG}" \
            --json > /tmp/songs.json
          python - <<'PY'
          import json
          from pathlib import Path

          def extract_count(path):
            payload = json.loads(Path(path).read_text())
            stack = [payload]
            while stack:
              item = stack.pop()
              if isinstance(item, dict):
                if "count" in item:
                  return int(item["count"])
                stack.extend(item.values())
              elif isinstance(item, list):
                stack.extend(item)
            raise SystemExit(f"No count field found in {path}")

          album_count = extract_count("/tmp/albums.json")
          song_count = extract_count("/tmp/songs.json")
          if album_count <= 0 or song_count <= 0:
            raise SystemExit(f"Remote D1 validation failed: albums={album_count}, songs={song_count}")
          print({"albumCount": album_count, "songCount": song_count})
          PY

      - name: Deploy Telugu Worker after successful import and validation
        run: |
          set -euo pipefail
          npx wrangler deploy --config "${GENERATED_WRANGLER_CONFIG}"

      - name: Mark deployed Telugu D1 slot as active
        env:
          GH_TOKEN: ${{ secrets.GH_VARIABLES_TOKEN }}
        run: |
          set -euo pipefail

          if [[ -z "${GH_TOKEN:-}" ]]; then
            echo "::error::Missing required secret GH_VARIABLES_TOKEN. Cannot mark the active D1 slot."
            exit 1
          fi

          if [[ "${TARGET_SLOT}" != "A" && "${TARGET_SLOT}" != "B" ]]; then
            echo "::error::Refusing to write invalid TARGET_SLOT '${TARGET_SLOT}'. Expected A or B."
            exit 1
          fi

          echo "Updating SRUTHI_TELUGU_ACTIVE_D1_SLOT to '${TARGET_SLOT}'."

          set +e
          gh api \
            --method PATCH \
            "repos/${GITHUB_REPOSITORY}/actions/variables/SRUTHI_TELUGU_ACTIVE_D1_SLOT" \
            -f name=SRUTHI_TELUGU_ACTIVE_D1_SLOT \
            -f value="${TARGET_SLOT}" >/tmp/slot-patch.out 2>/tmp/slot-patch.err
          patch_status=$?
          set -e

          if [[ $patch_status -eq 0 ]]; then
            echo "Updated SRUTHI_TELUGU_ACTIVE_D1_SLOT successfully."
            exit 0
          fi

          if grep -q "404" /tmp/slot-patch.err; then
            echo "SRUTHI_TELUGU_ACTIVE_D1_SLOT does not exist yet. Creating it now."
            gh api \
              --method POST \
              "repos/${GITHUB_REPOSITORY}/actions/variables" \
              -f name=SRUTHI_TELUGU_ACTIVE_D1_SLOT \
              -f value="${TARGET_SLOT}" >/tmp/slot-create.out 2>/tmp/slot-create.err
            echo "Created SRUTHI_TELUGU_ACTIVE_D1_SLOT successfully."
            exit 0
          fi

          echo "::error::Failed to update SRUTHI_TELUGU_ACTIVE_D1_SLOT."
          cat /tmp/slot-patch.err
          exit 1
